# 宅食図鑑 全面リデザイン 最終設計書（2026-08-27）

作成日: 2026-08-27
最新反映: 2026-08-28（P0〜P2-2適用後の現行UIに追従。実装状態を§6〜§14に反映）
作成者: Claude Code
基準文書: `docs/REDESIGN_COMPETITIVE_AUDIT.md`
範囲: 本ドキュメントは**設計＋現行UIのcanonical spec**。詳細な実測値・コード差分は既存監査doc（`docs/README.md`索引参照）を参照すること。
目的: 競合成功サイトの購買導線をBASELINEとして取り込み、当サイト固有の「値単位の出典・確認日・検証状況」という差別化資産を最大限可視化する最終設計を確定する。

---

## 1. リデザイン方針

**「比較 → 納得 → 信頼 → 公式サイトクリック」を最短化する。見た目の刷新は目的ではない。**

3つの原則を置く。

1. **競合が既に確立した購買導線パターン（結論の先出し・向いている人の早期提示・条件別導線・明確なCTA階層）は、独自性を主張せず素直に取り込む（BASELINE化）。**
2. **独自資産（値単位の出典・確認日・検証状況ダッシュボード）は、現状サービス詳細ページの奥に沈んでいる。これをトップページ・ranking.htmlという意思決定の早い段階まで引き上げる。**
3. **新しい機能を作ることを目的にしない。** 根拠のないランキング・★評価・ブラックボックススコア・偽の緊急性は作らない。3つの差別化候補（4章）以外に新規機能は追加しない。

---

## 2. 競合BASELINE（取り込みを決定した購買導線パターン）

`REDESIGN_COMPETITIVE_AUDIT.md`の実地調査に基づき、以下を**BASELINEとして全面的に取り込む**（独自性は主張しない、競合水準への追随として実施）。

| パターン | 根拠 | 取り込み先 |
|---|---|---|
| 結論を先に提示する | silver-choice.jp「迷ったらコレ」を本文前に先出し | TOP・ranking |
| 「向いている人」を価格より前に提示する | silver-choice・my-bestとも価格提示より前に配置 | service detail |
| 条件別の選択導線 | silver-choice「むせやすい→A社」等の条件直結、個人サイトの目的別TOP3カード | TOP・ranking |
| 価格・送料・キャンペーンの具体的な数値表示 | cachie・my-bestとも金額を明示。当サイトはcampaigns.htmlでは既に実現済みだがTOPページは未達（3章） | TOP（是正） |
| 比較表の視認性（強調表示） | my-best「第1位」バッジ、silver-choice「迷ったらコレ」 | ranking（ただしopaqueスコアは使わない、5章） |
| モバイルでの横スクロール＋案内文言 | silver-choice・coop-bento・takushoku-repoとも同一パターン、CRO知見でも比較表はカード化より横スクロールが適するとされる | 既存実装済み、変更不要 |
| 明確なCTA階層（内部リンクと公式サイトCTAの区別） | 全競合で共通。当サイトの「詳しく見る／公式サイトを確認」の2階層は既に整合 | 既存維持、回数のみ調整 |
| 診断→候補→詳細→公式という流れ | takushoku-repoのカード型TOP3、silver-choiceの条件別直結 | diagnosis（上位3件キャップ） |

---

## 3. 現行サイトの問題点（ページ横断・再確認）

> ※ 以下は**設計時点（2026-08-27）の所見の記録**。当時の問題の多くは現在の実装で解消済み（§6〜§13の「実装状態」を参照）。

`site/`配下の全ページを再度実際に開いて確認した結果（前回監査からサイト内容に変更なし、Phase4 commit `cc1852b`時点）。

| ページ | 現状 | 競合BASELINE | 問題 |
|---|---|---|---|
| TOP (`index.html`) | H1「宅配食を、データで選ぶ。」＋1行説明のみ。信頼性・実績の提示なし。11社が完全フラット表示。キャンペーン欄は「nosh 初回お試しキャンペーン」等ラベルのみで金額なし | 信頼性を最上部に、条件別導線、具体的な金額表示 | Hero相当が無い。差別化資産が不可視。キャンペーン表示が競合未満 |
| ranking.html | 「ランキング」を名乗るが`services.json`記載順のフラット一覧。保存方法フィルタのみ（Phase4実装済み） | 強調表示・条件別導線 | 名称と実態の不一致（4章で最終判断） |
| service detail（例: `syokurakuzen.html`） | 基本情報（価格含む）→特徴→良い点/気になる点→キャンペーン→送料/解約→関連サービス→比較→戻る、の順。CTA2回 | 向いている人を価格より前に、FAQ、CTA2〜4回 | 「向いている人」が基本情報テーブルの1行に埋没。FAQ皆無 |
| comparison（3ページとも同一構造） | 特徴/価格/対象/詳細リンク/公式CTAの表＋両社ポイント＋一般論の「まとめ」 | 差分強調・結論の先出し | 「どちらを選ぶべきか」の判断材料が無い |
| diagnosis | チェックボックス→スコア順に全件表示 | 上位数件への集約、一致理由 | 「あなたにはこの3社」という意思決定の代行になっていない |
| campaigns.html | サービスごとに割引タイプ・条件・最終確認日をカード表示、**確認済みのものは実額を明記済み**（例:「初回¥1,500 OFF＋継続割引」） | 具体的な数値表示 | **既に競合水準を満たしている。追加対応不要** |
| verification.html | 11社×3フィールドの確認状況一覧（バッジ＋出典リンク）。ページ冒頭に集計サマリーなし | 全社横断の透明性ページは競合に存在しない独自資産 | サマリー数値が無く、資産としてのインパクトが伝わりにくい |

---

## 4. 独自実装候補の最終検証

`REDESIGN_COMPETITIVE_AUDIT.md`で6条件テストに合格した3候補について、7項目で最終評価し、採用/却下を確定する。

### 候補1: トップページへの検証カバレッジ表示

| 評価項目 | 内容 |
|---|---|
| 意思決定の改善 | 「このサイト全体はどこまで裏付けがあるか」を3秒で把握できる。個別ページに行かないと分からなかった情報を最初に提示する |
| 競合に存在するか | **存在しない**（my-best/cachie/silver-choice/kurasim/coop-bento/takushoku-repoの6サイトいずれも全掲載社横断の確認状況集計は無い） |
| 模倣されにくさ | 高い。模倣するには競合自身が値単位の確認体制を持つ必要があり、単なる文言コピーでは再現できない |
| 運用コスト | 極めて低い。`verification.html`で既に計算しているロジック（`_price_status`/`_shipping_status`/`_campaign_status`）を再利用し、confirmed/derivedの件数を合計するだけ |
| データ品質への依存 | 依存する（現状33マス中の確認済み件数は少ないため、表示する数値自体は小さい）。ただし「少ない数字を正直に出す」こと自体が本サイトの一貫した方針であり、数値の大小は採用可否の判断材料にしない |
| CVへの期待効果 | 仮説段階。直接のCV要因ではなく、離脱防止・信頼形成という間接効果 |
| 実装難易度 | 低い。新規ページ・新規データ不要、既存関数の戻り値を1回集計するのみ |
| **判定** | **採用（GO）** |

### 候補2: 透明な基準による「全項目確認済み」バッジ

| 評価項目 | 内容 |
|---|---|
| 意思決定の改善 | 「11社の中でどれが一番裏付けが厚いか」が一目でわかる |
| 競合に存在するか | **存在しない**。my-bestの「検証スコア」は内訳非公開の独自採点（反面教師として明示済み）、silver-choiceの「迷ったらコレ」は編集判断であり機械的な基準ではない。「価格・送料・キャンペーンの3項目すべてを公式一次情報で確認できているか」という**検証可能な機械的基準**によるバッジは競合に無い |
| 模倣されにくさ | 高い。バッジを表示するには実際に3項目を確認する運用体制が要る（文言だけの模倣は虚偽表示になるため競合が安易に真似しにくい） |
| 運用コスト | 極めて低い。既存の3ステータス関数がすべて`confirmed`/`derived`を返すかの論理積を取るだけ |
| データ品質への依存 | 高い。現状this条件を満たすのは11社中2社（シェフの無添つくりおき・食楽膳。健康直球便は価格が`derived`のため、3項目すべて`confirmed`ではない点に注意——**「確認済み」の定義をconfirmed限定にするかderived許容にするかは実装時に確定する必要がある論点として明記**） |
| CVへの期待効果 | 仮説段階。アンカリング効果（数値・裏付けを伴う情報は意思決定に強く影響するというCRO知見）から一定の効果は期待できるが、当サイト固有の効果は未検証 |
| 実装難易度 | 低い。新規データキー不要、既存の3ステータス関数の組み合わせのみ |
| **判定** | **採用（GO）。ただし対象社が2〜3社と少数のため、「バッジが無い＝信頼できない」という誤解を生まないよう、バッジの意味（3項目すべてが公式一次情報で確認できている、というだけの意味であり総合評価ではない）を凡例で明記することを実装条件とする** |

### 候補3: 比較ページの差分自動強調

| 評価項目 | 内容 |
|---|---|
| 意思決定の改善 | 「AとBどちらを選ぶべきか」に直接答える（現状は一般論で終わっている、3章） |
| 競合に存在するか | **確認できず**（6サイトの調査で明確な「2社間の差分自動強調」は見当たらなかったが、対象範囲が限定的なため確信度は候補1・2より低いと明記する） |
| 模倣されにくさ | 中。仕組み自体は単純な集合演算・引き算であり、競合が本気で模倣すれば再現は容易。差別化の持続性は候補1・2より弱い |
| 運用コスト | 極めて低い。既存の`target`集合の差分演算、価格の引き算のみ。新規データ不要 |
| データ品質への依存 | 中。価格差表示は両社の価格が確認済み/算出値の場合のみ有効（現状3ペア中、食楽膳vs健康直球便のみ両社とも価格データあり。nosh関連2ペアはnoshが未確認のため価格差は出せない） |
| CVへの期待効果 | 比較ページ訪問者は意思決定の最終段階にいる高意図層であり、直接的な後押し効果が期待できる（他候補より確度が高いと判断） |
| 実装難易度 | 低い。3ページのみが対象、テンプレート1関数の追加で足りる |
| **判定** | **採用（GO）。ただし「模倣されにくさ」は他2候補より低いため、独自資産としての優先度は候補1・2より下** |

---

## 5. ranking.htmlの再設計（名称問題の最終判断）

### 5.1 「ランキング」を維持する根拠の検証

現状のranking.htmlは`services.json`の記載順（データ追加順）で11社を並べているだけであり、**価格・人気・満足度いずれの基準による順位付けも行っていない**。「ランキング」という名称は、ユーザーに「1位が最もおすすめ」という期待を抱かせるが、その期待に応える実装が存在しない。

検討した3つの選択肢:

| 案 | 内容 | 判定 |
|---|---|---|
| A. 現状維持（名称のみ「ランキング」を続投） | 何もしない | **却下**。名称と実態の不一致がそのまま残り、`SITE_UX_INTEREST_AUDIT_2026_08_26.md`以来の指摘が放置され続ける |
| B. 「全項目確認済み」を機械的な順位基準として採用し、確認度順に並べ替えて「ランキング」を名乗り続ける | 候補2のバッジ該当社を上位に固定表示 | **却下**。「確認できている項目数」は当サイト内部の作業進捗であり、「どのサービスがユーザーにとって優れているか」という価値判断ではない。これを根拠に1位・2位と序列化することは、ユーザーには「このサービスが一番良い」と誤読される可能性が高く、**本サイトが禁じてきた「根拠のないランキング」に該当するリスクがある** |
| C. ページの呼称を「ランキング」から**「比較一覧」**へ変更し、順位付けそのものをやめる | H1・タイトル・meta descriptionの文言変更のみ。URLは`ranking.html`のまま維持（SEOへの影響を避けるため） | **採用** |

### 5.2 決定

**ranking.htmlは「ランキング」という呼称をやめ、「宅配食 比較一覧」に改める。** 順位（1位・2位…）は一切付けない。行の並び順は現状の`services.json`記載順を維持する（これも恣意的な追加順であり、変更によって新たな序列を作らない）。

「全項目確認済み」バッジ（候補2）は各行の**事実表示**として設置するが、そのバッジの有無で行の並び順を変えない。バッジの凡例に「これは総合評価ではなく、価格・送料・初回キャンペーンの3項目を公式一次情報で確認できているかどうかの表示です」と明記し、序列化の意図が無いことを明確にする。

URL（`ranking.html`）・ナビゲーションのリンク先は変更しない。変更するのはページ内のH1・タイトルタグ・meta description・本文中の「ランキング」という文言のみ（実装時にnav文言「おすすめ比較」も「比較一覧」に統一するかは実装時に判断、本設計では必須としない）。

---

## 6. TOP設計

**購買導線: 誰向けか → 何を比較できるか → どれだけ検証しているか → 候補を見る → 詳細 → 公式**
**実装状態: 実装済み（9393854・0f9abd8・6fc80d4・71f62b0・ba28d4b・819181e 等で反映）**

| 要素 | 現行設計（実装済み） |
|---|---|
| ファーストビュー | H1「宅配食、どれを選べばいい？」＋リード文「価格・送料・初回キャンペーンを1項目ずつ、公式サイトで確認しています。」＋**hero-figure**（16社という大きな数字）＋hero-actions（診断ツール[btn-primary]／一覧で比較する[hero-sub-cta]） |
| 最初に見せる情報 | **目的で探す**（`purpose-section-top`＝チップ群）を入口に配置。PCは常時展開、モバイルは`<details>`折りたたみ。**色帯は透明化**し、heroの色帯を除きファーストビューを明るくする（P2-2） |
| 検証カバレッジ | 集計ダッシュボード（3数値・進捗バー）は**verification.htmlに集約**。TOPは**数字を含まない1行の`trust_line()`**＋「確認状況を一覧で見る」リンクのみに縮小（数字の反復を排除、P0-A） |
| 主要サービス一覧 | 見出しは太罫線（`heading-rule`）。**2列グリッド＋罫線区切りリスト化**（脱カード化、P0-B/P1-2）。カード＝サービス名→1食あたり価格（検証バッジ同居）→向いている人（1行）→タグ→CTA（詳しく見る[btn-primary]／公式サイト[text-link]）。**段階開示**: 1〜4枚目常時／5〜6枚目PCのみ／7枚目以降`<details>`「残り◯社をすべて見る」 |
| CTA | 発見段階の主役は診断ツール[btn-primary]。一覧を見た後の救済導線は「診断ツールで絞り込む →」[text-link]。公式サイトCTAは`text-link`（情報不足の段階では外部CTAを主役にしない） |
| 初回キャンペーン | `campaign-pick`。確認済みを優先ソートし、先頭1件を**PICK UP（非対称）＋色帯**で大きく、続く2件を小リストで表示。実額（discount_type）を表示 |
| このサイトのこだわり | `section-flat`（影・枠線なしのフラット面）で脱カード化。3項目（鮮度／公式確認／目的別） |
| セクション余白 | **不均一化**（P1-1）し、意図的なリズムを作る |
| 内部回遊 | 比較一覧（/ranking）・診断（/tool/diagnosis）・キャンペーン（/campaigns）・検証状況（/verification）への導線を集約 |
| モバイル表示 | 目的は折りたたみ、サービス一覧は段階開示。カードは縦積みの範囲内（新規CSSコンポーネント不要） |

---

## 7. ranking設計（比較一覧、5章の名称変更を反映）

**実装状態: 実装済み（d3e7a58・71f62b0・819181e・P0-A 等で反映）**

| 要素 | 現行設計（実装済み） |
|---|---|
| ファーストビュー | H1「宅配食 比較一覧【2026年8月最新】」＋目的別チップ（TOPと共有）＋説明文（順位付けをしない旨） |
| 最初に見せる情報 | 目的別チップ → 保存方法フィルタ（任意）→ **表示価格が安い順ソート**（任意・既定オフ）→ 凡例 → 比較テーブル |
| 比較/選択UI | 6列テーブル（サービス／**1食あたりの料金**／初回キャンペーン／保存方法／向いている人／CTA）。`<thead>`＋**stickyヘッダー**。**価格数字の縦位置揃え**（tabular-nums・右寄せ）。「全項目確認済み」バッジ（序列化しない）。各行に「気になる点（1例）」を併記 |
| CTA | 2階層（詳しく見る[btn-primary]／公式サイトを確認[btn-secondary]） |
| 検証カバレッジ | 集計ダッシュボードは廃止し、**比較表より後**に1行の`trust_line()`（数字あり・16社×3項目=48の導出を明記）＋「全項目確認済み」の凡例注記 |
| 内部回遊 | 選び方のポイント（千鳥罫線）＋診断CTA、比較ペアブロック |
| 出典/確認日 | セル内の確認状態バッジ＋出典リンク（既存維持）。検証カバレッジの数値は`verification.html`と同一集計 |
| モバイル表示 | **カード縦積み**（価格→保存方法/向いている人→検証→CTA）。テーブルはデスクトップのみ |

---

## 8. service detail設計

**実装状態: 実装済み（bb1ef36・21147a5・71f62b0 等で反映）**

| 要素 | 現行設計（実装済み） |
|---|---|
| ファーストビュー | H1（サービス名）＋tags＋**確認日**（page-head、`last_checked`）＋良い点[0]の一言（`pros-highlight`） |
| 最初に見せる情報 | **「こんな方におすすめ」**（`service_recommend_block`）を価格より前に独立カード（panel-accent）として提示。基本情報テーブルから「対象」を分離し、ここに集約 |
| 料金表示 | **display価格を主表示＋全価格ポイント一覧**（`pricing_detail_html`）。未確認の値は小さく控えめ、確認済みは`--price-figure`で強調 |
| ページ構成 | 料金→（凡例）→基本情報（運営会社/形態/保存方法）→初回キャンペーン→解約・送料→特徴→良い点・気になる点→**よくある質問（FAQ）**→関連サービス→比較ページ |
| CTA | 3回（料金カード内・初回キャンペーンカード内・ページ末尾）＋各所にvstatus凡例 |
| 出典/確認日 | 値単位のバッジ＋出典リンク（既存維持）。FAQは`cancellation_note`/`shipping.notes`/`target`を`<details>`アコーディオンで再構成 |
| モバイル表示 | 既存の`.card`構造の範囲内（新規コンポーネント無し） |

---

## 9. comparison設計

**実装状態: 実装済み（21147a5・71f62b0・P0-A 等で反映）**

| 要素 | 現行設計（実装済み） |
|---|---|
| ファーストビュー | H1（A vs B）＋**結論パネル**（panel-accent）: 価格差（基準一致＋confirmed/derived限定の自動算出）＋**向いている人の集合差分**。片方未確認・基準不一致・レンジの場合は単純な「◯円安い」を出さず文言で説明 |
| 向いている人 | **箇条書き**（`<ul class="feature-list">`）で「Aが向いている人／Bが向いている人／どちらでも当てはまる人」を表示 |
| CTA | **公式サイトCTAは結論パネルのみ**（比較表・下部フッターに重複させない）。詳細ページ・キャンペーン・診断への内部導線は下部に配置 |
| 比較/選択UI | 比較テーブル（特徴/1食あたりの料金/向いている人/詳細リンク）。確認状態バッジ・価格ラベルを価格セルに表示 |
| 出典/確認日 | 比較ページ自体に個別の出典リンクを追加しない（既存方針維持）。確認状態は凡例で示す |
| モバイル表示 | 横スクロール＋案内文言を維持 |

---

## 10. diagnosis設計

**実装状態: 実装済み（21147a5・71f62b0・ba28d4b 等で反映）**

| 要素 | 現行設計（実装済み） |
|---|---|
| ファーストビュー | H1「宅配食 診断ツール」＋**期待値提示**（「目的・保存方法を選ぶだけで、条件に近い上位3社をすぐに表示します」）＋目的チェックボックス＋保存方法チェックボックス＋選択サマリー |
| 比較/選択UI | 結果は**スコア上位3件にキャップ**し、各社に**「一致した条件」**（一致したtag名の強調表示）＋価格・検証状態を表示。4件目以降は「他にも◯件が条件に一致→比較一覧で全件見る」へ誘導 |
| CTA | 各結果カードに詳しく見る[btn-primary]／公式サイトを確認[btn-secondary] |
| タグ表示 | 保存方法カテゴリは**最小ラインアイコン**＋属性別の視覚区別（`tag-storage`） |
| 計測 | GA4（diagnosis_start・diagnosis_complete）。選択サマリーは純粋な表示のみ（スコアリングに影響しない） |
| 出典/確認日 | 診断結果には出典リンクを付与しない（既存方針維持） |
| モバイル表示 | 既存のまま（結果が3件に絞られるため視認性は改善） |

---

## 11. campaigns設計

**実装状態: 実装済み（ba28d4b・VEA項目3/3b で反映）**

確認済みのキャンペーンは実額（discount_type、例:「初回¥1,500 OFF＋継続割引」）を表示し、未確認のものは「公式確認中」と正直に表示する（設計方針は維持）。2026-08-28時点で、先頭1件は**PICK UPカード（`campaign-pick-featured`）**として非対称に大きく表示し、確認済みを優先ソートする（TOPの`campaign-pick`と同一ロジック）。キャンペーン情報は毎週更新（`LAST_VERIFIED_DATE`明記）。

---

## 12. verification設計

**実装状態: 実装済み（P0-A・819181e で反映）**

| 要素 | 現行設計（実装済み） |
|---|---|
| ファーストビュー | H1「全◯社の価格・送料・キャンペーン確認状況」＋**集計サマリー**（`trust_panel()`・3数値＋進捗バー＋凡例）は**本ページにのみ**配置 |
| 最初に見せる情報 | 集計サマリー→凡例→◯社×3フィールドの一覧表（価格/送料/初回キャンペーン＋出典リンク・確認日） |
| 検証カバレッジの集約 | **集計ダッシュボードはverification.htmlに集約**。TOP・rankingは`trust_line()`（1行＋リンク）に格下げし、数値は本ページと同一集計ロジックを共有 |
| 比較/選択UI | 変更なし（スコアやランキングを付けない、既存方針を維持） |
| CTA | 本ページは非CTA。サービス名リンク→サービス詳細（既存） |
| 内部回遊 | TOP・rankingのtrust_lineから直接リンク |
| 出典/確認日 | 既存の出典リンク一覧を維持（`sources.json`を直接参照しない設計は不変） |
| モバイル表示 | 横スクロール＋案内文言を維持 |

---

## 13. 共通UI/CTA設計・モバイル設計・分類まとめ

### 13.1 CTA設計
- 2階層（詳しく見る[btn-primary]／公式サイトを確認[btn-secondary or text-link]）を維持する。**作り直さない**。
- 階層は情報段階に応じて適用: 発見段階（TOP）の公式CTAは`text-link`、比較・詳細段階は`btn-secondary`。
- サービス詳細は3回（料金／初回キャンペーン／末尾）。比較ページの公式CTAは結論パネルのみ（重複させない）。
- **価格公平性UI（d3e7a58）**: 初回/お試し価格を視覚的に強調し、「表示価格が安い順」の並び替えを追加（任意・既定オフ）。これは順位付けではなくユーザー起点の並び替え。
- CTA文言への実額反映（例:「初回26%OFFで公式サイトを見る」）は**DATA FIRST/計測待ちのためDEFER**（方針維持）。TOPキャンペーン欄の実額反映（6章）は情報の正確性の是正として先行実施済み。

### 13.2 モバイル設計
- **比較一覧（ranking）はモバイルでカード縦積み**（`ranking-mobile`）。デスクトップはテーブル。
- **比較ページ・verificationは横スクロール＋案内文言**を維持（`mobile_scroll_hint`）。
- sticky CTAは実装しない（競合6サイトに実装なし、根拠不十分、DEFERを追認）。
- 出典・確認日は折りたたまない（差別化情報を隠さない）。
- **デザイントークン体系**（`_CSS` `:root`）を導入済み: `--color-*`／`--space-*`／`--radius-*`／タイポグラフィ。新色は追加しない。
- タグは属性別に区別（`tag-storage`＝保存方法は最小ラインアイコン＋色区別、その他は通常タグ）。

### 13.3 最終分類

| 分類 | 項目 |
|---|---|
| **MUST MATCH** | TOPページのキャンペーン表示に実額を反映／service detailの「こんな方におすすめ」早期セクション／TOPの目的別導線カード |
| **COPY BASELINE** | service detailのCTA回数を2→3に／確認日のH1直下再掲／diagnosis結果の上位3件キャップ＋一致理由／FAQセクション新設（確信度中） |
| **DIFFERENTIATE** | 検証カバレッジ表示（TOP・ranking・verification共通ロジック）／「全項目確認済み」バッジ（ranking、序列化はしない）／比較ページの差分自動強調（結論の先出し・向いている人の差分） |
| **DATA FIRST** | ranking（比較一覧）の価格ソート・フィルタ／CTA文言の実額反映／診断ツールの価格フィルタ |
| **DO NOT BUILD** | ranking.htmlの数値順位付け（★評価・スコア・確認度による序列化を含む、5章）／sticky CTA／モバイルのカード化／出典・確認日の折りたたみ／偽の緊急性演出／推薦エンジン・matching DSL |

---

## 14. 実装ロードマップ（Phase5想定）

> **実装状況（2026-08-28）: Step 1〜3はすべて実装済み**（§6〜§13の「実装状態」を参照）。Step 4は「価格ソートの並び替えUI」のみ前倒し実装、他は保留。

### Step 1（MUST MATCH、最優先・低コスト）— **実装済み**
1. TOPページのキャンペーン表示にcampaigns.jsonの実額を反映
2. service detailの「こんな方におすすめ」早期セクション
3. TOP・rankingの目的別導線カード（共有コンポーネント）

### Step 2（DIFFERENTIATE本体）— **実装済み**
4. 検証カバレッジ集計ロジックの新設（TOP・ranking・verification.htmlで共有）
5. ranking.htmlの名称変更（「ランキング」→「比較一覧」）と「全項目確認済み」バッジ
6. comparisonページの結論先出し・向いている人の差分表示

### Step 3（COPY BASELINE、低優先度）— **実装済み**
7. service detailの確認日再掲・CTA3回目・FAQセクション
8. diagnosis結果の上位3件キャップ＋一致理由

### Step 4（保留・データ/計測待ち）— **一部前倒し実装**
- ranking（比較一覧）の価格ソート・フィルタ（price confirmed 6/11社到達後） → **「表示価格が安い順」の並び替えUI（任意・既定オフ）はd3e7a58で実装済み**。目的別・価格帯フィルタは未実装
- CTA文言の実額反映（GA4ベースライン計測後） → 未実装（DEFER維持）

### 恒久非実装
- ranking.htmlへの数値順位付け・★評価・ブラックボックススコア（5章の最終判断）
- sticky CTA、モバイルカード化、出典/確認日の折りたたみ、偽の緊急性、推薦エンジン／matching DSL

※ 恒久非実装の判断は維持する（2026-08-28時点で変更なし）。

---

## 付録A: 実装経緯（2026-08-27〜08-28・参照用）

本specの§6〜§14が反映する実装の履歴。詳細な根拠・実測値・コード差分は各doc（`docs/README.md`索引）を参照。

| 実装 | 反映コミット | 根拠doc |
|---|---|---|
| 全面リデザイン・デザイントークン・モバイル比較カード化 | bb1ef36 | REDESIGN_IMPLEMENTATION_REPORT.md |
| 価格schema再設計（pricing+price_points） | 9ec213e | REMAINING_8_ITEMS_AUDIT_20260827.md |
| 15社化・CTA階層・モバイルカード・価格表示改善 | 7247b42・15ca7e7・d74ea46・1967bc2・77f3f60・2f5b413 | REAL_BROWSER_UI_AUDIT_2026_08_28.md |
| 診断結果の価格・比較CTA重複整理・食欲喚起導線 | 21147a5 | APPETITE_APPEAL_UI_AUDIT / PURCHASE_FUNNEL_UI_AUDIT |
| TOPレイアウト再設計・段階開示・P2改善 | 9393854・0f9abd8・6fc80d4 | TOP_LAYOUT_* / TOP_P2_* / TOP_PROGRESSIVE_* |
| URL正規化（拡張子なし） | 3d5952d | URL_NORMALIZATION_* |
| わんまいる追加・アフィリエイトリンク | 28e49b8・2ea8ed2 | WANMAIRU_* / AFFILIATE_LINK_UPDATE_QA |
| 価格公平性UI（初回/お試し強調・安い順ソート） | d3e7a58 | COMPETITOR_UX_FUNNEL_AUDIT / PRICE_FAIRNESS_UI_FIX_FINAL_AUDIT |
| Visual Design System 8項目 | 71f62b0 | VISUAL_DESIGN_SYSTEM_* |
| Visual Expression 7項目 | ba28d4b | VISUAL_EXPRESSION_ANTI_GENERIC_* |
| P0〜P2-2（カバレッジ集約・グリッド・余白・色帯） | 819181e | VERIFICATION_COVERAGE_AND_GRID / P1 / P2_0_2_1 / P2_2 |
